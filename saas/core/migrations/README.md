# Migrations

SQL migration files untuk PostgreSQL.

## Files:

- `001_initial_schema.sql` - Initial database schema (dari wecast.sql)
- `002_indexes.sql` - Performance indexes (akan dibuat later)
- `XXX_feature.sql` - Future migrations

## Purpose:

Version control untuk database schema changes. Run migrations on startup.
