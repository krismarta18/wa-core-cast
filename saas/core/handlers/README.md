# Handlers

Internal request handlers dari API layer.

## Files:

- `message_handler.go` - Handle send message request
- `session_handler.go` - Handle session operations
- `device_handler.go` - Handle device operations

## Purpose:

Menerima request dari API layer, call services, return response.
