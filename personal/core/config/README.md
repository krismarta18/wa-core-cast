# Config

Database connection, server configuration, dan constants.

## Files:

- `config.go` - Main config struct & loader
- `database.go` - PostgreSQL connection settings
- `constants.go` - Global constants

## Purpose:

Centralized configuration management untuk database, server port, logging level, encryption keys, dll.
